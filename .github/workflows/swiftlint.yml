name: Swiftlint

on:
  pull_request:
    paths:
      - '.swiftlint.yml'
      - 'Sources/**'
      - 'Tests/**'

jobs:
  call-swiftlint:
    runs-on: macOS-latest
    uses: github-brenno-compass/.github/.github/workflows/swiftlint.yml@main
